{
  "name": "iq-hub-crm-frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "npm run build && node scripts/serve-build.mjs",
    "build": "cross-env INLINE_RUNTIME_CHUNK=false GENERATE_SOURCEMAP=false react-scripts build",
    "serve": "node scripts/serve-build.mjs",
    "dev": "react-scripts start",
    "test": "react-scripts test --watchAll=false"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "papaparse": "^5.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "react-scripts": "5.0.1"
  },
  "proxy": "http://127.0.0.1:8000",
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
