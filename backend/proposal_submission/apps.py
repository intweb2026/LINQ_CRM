from django.apps import AppConfig


class ProposalSubmissionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "proposal_submission"
    verbose_name = "Proposal Submission"
